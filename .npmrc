# Prisma 7 generates its client into src/generated/prisma, which resolves
# modules from the project root. pnpm's strict linking hides Prisma's runtime
# packages there, so hoist them where the generated code can see them.
public-hoist-pattern[]=@prisma/*
public-hoist-pattern[]=prisma
